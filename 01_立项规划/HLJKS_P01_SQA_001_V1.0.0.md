# 质量保证计划（SQA）

- **文档标识号**：HLJKS-SQA-001-V1.0
- **状态**：APPROVED ｜ 质量记录：`docs/REGRESSION.md`、`docs/COMPLIANCE.md`、`AI-CODING.md`

---

## 1. 质量目标

| 目标 | 度量 | 达标值 | 本期实测 |
|---|---|---|---|
| 闭环可演示 | Mock 模式下全链路（出题→作答→判题→解析） | 100% 可走通 | ✅ e2e 8/8 |
| 单元/服务层质量 | `npm run verify` 断言通过率 | 100% | ✅ 32/32 |
| 接口正确性 | e2e 契约用例（400/200/错误体/字段） | 100% | ✅ 8/8 |
| 构建质量 | `npm run build` | 零错误 | ✅ 101ms |
| 文档完整性 | 常驻文档 + A 级文档族 | 一个都不能少 | ✅ 24 份 |
| 真实性 | 未验证项声明 | 无夸大 | ✅ 见 TSR §5 |

## 2. 评审与验证策略（原子测试二值结论）

| 层 | 验证手段 | 通过标准 | 不通过处置 |
|---|---|---|---|
| 服务层 | `node scripts/verify-services.js` | 32 项全绿 | 立即修复，3 次未成则换方案 |
| 接口 | `node test/e2e.mjs`（自启 3199） | 8/8 PASS，退出码 0 | 同上 |
| 前端 | `npm run build` | 零错误 | 同上 |
| 界面原子性 | 按钮 handler 全量核验（12 处）+ 冗余声明清查 | 无死按钮、无冗余 v-show | 删除/补 handler 后复验 |
| 需求符合性 | `docs/COMPLIANCE.md` 逐条矩阵 | 全部 ✅（除已声明项） | 补实现或如实声明 |

## 3. 评审计划（波次评审）

| 评审点 | 对象 | 方式 |
|---|---|---|
| 每波次末 | 该波次全部产物 | 主 Agent 原子测试后放行（AGENTS.md §8） |
| Wave 7 | 全量代码 + 文档族 | 三连回归 + 二次代码验证（对照 SRS/LLD/STD） |

## 4. 质量记录要求

- 所有"已完成"标注必须附原子验证证据（C6），截图/日志留痕于模块说明或 REGRESSION 文档（AGENTS.md §5）。
- 缺陷必须编号连续递增、可追溯（`04_实现测试/HLJKS_P04_BUG_001` BUG-001~007，状态 CLOSED）。
- AI 错误记录只写实时补录，禁止事后编造（AI-CODING.md §0.2）。

## 5. 与 AGENTS.md 的质量约束对照

| 约束 | 落实方式 |
|---|---|
| C3 统一 Schema | 所有数据交互走 `docs/DATA_SCHEMA.md`，接口返回统一错误体 `{error:{code,message}}` |
| C4 Mock 双轨 | 无 Key/超时/异常必须回退 Mock 并携带 `mock:true`（callLLM.js 唯一调用点） |
| C6 完成定义 | 模块未过原子验证不得标记完成（本项目无此情况） |